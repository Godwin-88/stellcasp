[profile.release]
opt-level = "z"          # Optimise for size (smaller WASM = cheaper deploy)
overflow-checks = true   # REQUIRED by Soroban — prevents silent integer overflow
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true